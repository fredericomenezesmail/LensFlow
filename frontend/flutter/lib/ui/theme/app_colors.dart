import 'package:flutter/material.dart';

class AppColors {

  // Azul principal (AppBar, Navigation)
  static const Color primary = Color(0xFF1976D2);

  // Azul claro (fundo geral)
  static const Color background = Color(0xFFE3F2FD);

  // Branco para superfícies
  static const Color surface = Colors.white;

  // Branco para barra de navegação
  static const Color navigationBackground = Colors.white;

  // Texto principal
  static const Color textPrimary = Colors.black;

  // Texto em fundo escuro
  static const Color textOnPrimary = Colors.white;

  // Ícone do coração
  static const Color favorite = Colors.red;

}